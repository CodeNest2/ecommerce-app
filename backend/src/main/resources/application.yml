spring:
  datasource:
    url: jdbc:h2:mem:ecommerce
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: never # Disables execution of schema.sql and data.sql
server:
  port: 8081
stripe:
  apiKey: 123456789999000000000000000000
 
